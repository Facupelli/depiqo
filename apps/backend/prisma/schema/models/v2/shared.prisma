// ---------------------------------------------------------------------------
// V2 SHARED ENUMS
// ---------------------------------------------------------------------------

enum V2TrackingMode {
  IDENTIFIED
  POOLED
}

enum V2FulfillmentMethod {
  PICKUP
  DELIVERY
}
